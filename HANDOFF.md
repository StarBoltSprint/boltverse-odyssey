# HANDOFF — one joint, not a third engine

Hall just finished **Enter** (`kind: sprint`). Lane starts a **calm** minute. Between them: curtain + clock reset. Same 40/20/40.

Hall Enter: [ENTER.md](ENTER.md). Lane: [PLAY.md](PLAY.md). Link: [LINKS.md](LINKS.md).

## In (`ended(enter)`)

Must: `coming === false`, `calm-1` on disk, enter last = fill/veil (**never** Lane spawn). Else 2nd tap = **stay**.

| | at hid playing |
|---|---|
| `m` | **0.12** — do not carry hall `m` |
| `t_run` | **0** |
| `tier` | calm |
| `kind` | `sprint` |

Tapping doors well ≠ start in lean. Last(enter) **kisses** first(calm-1).

```
hall-enter + ended     → handoff
handoff + hid playing  → lane-running
handoff + play-fail    → decay, stay, no cook
```

## Out (return) — handoff reversed

Not `ended(peak)`. Not low `m`. A **2nd armed tap** + a hung edge.

```
RETURN = { kind: "hall", to: "moss", clip: "films/return-forest-moss.mp4" }
```

On the **Lane** row / LINKS — not `adjacency.json` (that is hall→hall).

Year-0: end of minute (`t_run > 70` or terminal decay) → Lane hold + **one** tap if the edge is hung. Peak does **not** arm. Late does not arm. Wrong side = Miss, no teleport.

Not hung → stay decay / Recall `/`. No cook. No chrome « hall » button. Honest and ugly.

Stock `return-*.mp4` = free tap (like a walk). Live cook = ticket, same family as Enter. Backing off does not spend.

### Picture

```
first(return) = last still of the current Lane plate (or decay hold)
last(return)  = veil / fill — NOT hall spawn in the shot
under veil    = dest hall spawn still
```

Same ENGINE curtain: hide 0, empty veil of **that** side, dest breath-spawn, double rAF 500 ms. Never last(peak) = hall spawn (two dogs / two sets).

### Clocks at `ended(return)`

`kind = hall` · `m` **dies** · `t_run = 0` · pose = dest spawn · open = dest `breath-spawn`. Do not carry peak into the citadel.

```
lane-running + arm + 2nd tap → lane-return
lane-return + ended          → hall-breath dest
lane-running + play-fail     → Lane decay, stay
```

## Refuse

Auto-return at `t_run > 70` · WASD · recook moss on the exit tap · `playbackRate` 1.1.

## One line

**Enter poses the dog; calm-1 makes him run; return is another threshold.** No clip = you stay in the forest. The storm dies at the join. Peak does not push the door.
