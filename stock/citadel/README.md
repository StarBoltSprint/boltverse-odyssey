# stock/citadel — sole cold-start Welcome teaser

SmiR’s epic citadel + Bolt (crystal citadel + golden heart). **This is the only mp4 attached on *play / lance Boltverse Odyssey*.** Not the old frost hall loop.

| file | what |
|---|---|
| [`preview-loop.mp4`](preview-loop.mp4) | **sole** cold-start Welcome teaser (~10s, 1280×720, 16:9, H264). Attach this one only. 16:9 for now; locked-off / 9:16 recook later — do not block. |
| [`preview-first.jpg`](preview-first.jpg) | first-frame poster |

**Chat:** attach this **one** mp4 as Welcome media (not a tap / not a hitbox). Never attach the Sprint / biome pair at boot.  
**Play:** loop forever (`muted playsInline autoPlay loop` + watchdog re-`play()` on `pause` / `ended`).

Biome preview stays on disk as an optional archive — **not attached at boot**: [`stock/biome/preview-loop.mp4`](../biome/preview-loop.mp4). Sprint cook = [`biome/`](../../biome/README.md).

Not a hung pack. Not `cook-room`. Hang / cook laws unchanged.
