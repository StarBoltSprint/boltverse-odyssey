`bolt-back.jpg` is the Pack Bolt **style** source of truth for Sprint biomes. Strict rear, fluffy white German Shepherd, black void, rim light. Same bytes as [`biome/lock/bolt-back.jpg`](../biome/lock/bolt-back.jpg) — either path OK. **PRIORITY 0 TEACHER GATE:** attach / show this exact image in the Build chat before any Bolt still/clip, then `@ref` it. If the teacher is not shown → **STOP. No cook. FAIL.** Soft KEEP banned without teacher proof. The black void is STYLE only — biome cooks still go to flat `#00FF00` plus light bake from the empty plate. Hang ≠ wipe.

**HARD BAN as identity sources** (never `@ref` as Sprint biome Bolt teacher):
- `biome/master/bolt.mp4` / hung bolt — OUTPUT only
- `bolt-back-prev.jpg` — previous standing teacher (archive only — keep the file, never `@ref`)
- `RIG-*` / `SEAL-*` / `example-*` / `sill-*` — Citadel hall locks, **NOT** Sprint biome Bolt teacher
- Any local `bolt-rear-*.jpg` invented in a Build sandbox

---

## Rotary-gallop cycle (motion teacher — sealed 2026-09-20)

| File | Role |
|---|---|
| [`bolt-gallop-cycle.mp4`](bolt-gallop-cycle.mp4) | **CANONICAL** 1s hard-cut closed rotary-gallop (24fps, 768×1168, flat `#00FF00`, strict rear, ×2 cadence). Cook source. |
| [`bolt-gallop-cycle-first.jpg`](bolt-gallop-cycle-first.jpg) | First frame of that cycle. Required **first AND last** still for Imagine I2V / `imagineBoltClip` green loops. |
| [`bolt-gallop-cycle-12s-preview.mp4`](bolt-gallop-cycle-12s-preview.mp4) | Optional 12s looped preview of the same cycle. **Preview only — not the cook source.** |

**HARD — STEP 0 MOTION GATE:** every new biome Bolt cook **must** attach / show [`bolt-gallop-cycle.mp4`](bolt-gallop-cycle.mp4) (or its first frame [`bolt-gallop-cycle-first.jpg`](bolt-gallop-cycle-first.jpg)) as **motion teacher BEFORE** cooking Bolt. Style teacher remains [`bolt-back.jpg`](bolt-back.jpg). Two different files. Both required.

Green Bolt cycle cooks use `bolt-gallop-cycle-first.jpg` as **first AND last** frame. Loop seam = **hard cut** on the closed period. Do **NOT** optical-flow morph last→first.

**BAN** inventing a new gallop from chat Imagine without this teacher. If the motion teacher is not shown → **STOP. No cook. FAIL.**

This hang does **not** wipe `biome/master/bolt.mp4`. Hang ≠ wipe.
