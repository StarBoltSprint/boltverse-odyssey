# xAI Imagine API — live cook only. Never commit the real key.
# Dry-run does not need this:
#   node scripts/cook-room.mjs moss --dry-run
# Hung PASS stills/films are reused. Recook anyway:
#   COOK_FORCE=1
XAI_API_KEY=
