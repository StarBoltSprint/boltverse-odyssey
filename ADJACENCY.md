# ADJACENCY — legal Hall′ (not hung, not sprint)

`adjacency.json` = who **may** sit behind a door **before** Enter is cooked.

| File | Question |
|---|---|
| this json | is `to` a catalog hall slot? |
| `room.json` ENTER | is the clip hung? |
| `biomes.json` | sprint Lanes only |

Floor 1 (`citadel dusk`) does **not** read this.  
`open door A of dusk` → `edges.dusk.A` (`moss`). Then [ENTER.md](ENTER.md).  
`to: "temple under the sea"` → FAIL.  
Stock / citadel golden: both doors `null`.  
`from === to` illegal. Return is another line, not an auto-mirror.

Player never loads this file. The hung pack wins at tap. This map wins at **cook**.
