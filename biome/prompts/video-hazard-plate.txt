WIDTH LAW (all 10 seconds): hazard stays in the SAME one or two lanes as the first frame. It may get closer (taller) but NEVER wider. Never a wall, never curb-to-curb, never a gantry, never a shock-ring, never extra copies. It exits the BOTTOM still one/two lanes wide — no sideways smear. LEFT and/or RIGHT dashed paint stays visible at t=0, mid, and last. If all three lanes are blocked at any frame, FAIL. {PAINT}
