# assets — optional biome extras

Optional stills / loops that are **not** the `/master` stack and **not** the console teaser.

Keep the B-stack law: empty plates stay empty; Bolt stays a cutout. No wallet art. No TAP stamps.

If nothing is hung yet, leave this folder as a pointer.
