# 29 — Imagine is the level compiler, not the game engine

Cook gate. Living-film split stays: empty (or hazard) plate + GPU Bolt ([10](10-bolt-cutout-law.md) · [15](15-gpu-compositor.md)).

## Split

```
stills (m stack)  →  clip 10s (first + mid + last + @refs)  →  GPU (road + keyed Bolt + FX)
                         ↑
                  Imagine does not play
                  Imagine BAKES the world
```

- **Still = source of truth.** Video interpolates. Do not prompt the clip to invent.
- **Bolt never in the world cook.** Sealed gallop cycle. Jump / swipe / plant = player.
- **Camera lock 1-point.** Spectacle = the **event**, not a new angle ([24](24-camera-1point.md) · [25](25-hazard-cone.md)).
- Dodge + cinema together: event in **1–2 lanes in the air**; décor on **sides/sky**; always one free corridor; asphalt stays dark.

## Do not

- Treat Imagine as Unity (realtime loop, L/C/R films, native Bolt default).
- Prompt “world ADVANCED / cinematic / magnificent” on the last frame → crystal pile.
- Cook Pn from P0 empty if décor stills `1…n-1` exist.
- Hang without QC on frames 0 / 25 / 50 / 99 %: VP stable, road luma, free corridor, zero dog ([23](23-plate-geo-qc.md)).
- Seam fail: `last(n)` must match `first(n+1)` as **still identity** (same WB, same cone, décor closer), not “it looks similar”. Measure it: law [33](33-plate-mae-qc.md) `plate-mae-qc.py` before hang. Exit non-zero = do not hang.

## Native-slide ([27](27-native-road-slide.md))

OPTIONAL catalog. GPU is better for a 10-plate `m` sprint (N+1, jump, dodge, one cycle). Native is a hero-shot lab: needs `native-C` + `empty-C` + baked matte, dashes **never under paws**, or the center-lane ghost stays. Cold Grok must not switch a biome to this.

## Related

[00](00-PRIORITY0-any-biome.md) · [10](10-bolt-cutout-law.md) · [22-m](22-m-densify-snowball.md) · [28](28-stills-two-rails.md) · [30](30-i2i-prompt.md) · [32](32-howl-gpu-targets.md) · [33](33-plate-mae-qc.md)
