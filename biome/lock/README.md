# biome/lock — Bolt teacher (same bytes as repo-root `lock/`)

`bolt-back.jpg` **is** [`lock/bolt-back.jpg`](../../lock/bolt-back.jpg). Same file. Either path OK.

**PRIORITY 0 TEACHER GATE:** attach / show this exact image in the Build chat before any Bolt still/clip. Then `@ref` it. If the teacher is not shown → **STOP. No cook. FAIL.** Soft KEEP banned without teacher proof.

`STEP 0: attach lock/bolt-back.jpg (or biome/lock/bolt-back.jpg) in chat before cooking Bolt.`

**PRIORITY 0 — Bolt sprint for new biomes = REUSE, not invent.** Take sealed [`lock/bolt-gallop-cycle.mp4`](../../lock/bolt-gallop-cycle.mp4) as the Bolt motion asset (**CANON ~5.56 s / 534 frames / 96 fps**). Same bytes as `lock/bolt-gallop-cycle-96fps-loop6s.mp4`. Play: loop 1×; no per-frame seek; no 534-canvas harvest. Old `lock/bolt-gallop-cycle-0.93s-prev.mp4` = **ARCHIVE — FAIL as play cycle**. Key + despill + composite onto Video A. **FAIL** if Grok invents a new Bolt sprint clip. Only SmiR can authorize a new cycle cook to replace the lock. Style teacher stays `bolt-back.jpg` if a still / repose is needed. Empty-road plates stay **48 fps**. Preview only: [`lock/bolt-gallop-cycle-12s-preview.mp4`](../../lock/bolt-gallop-cycle-12s-preview.mp4). Files live under repo-root `lock/` (pointer here). Law: [15-gpu-compositor](../docs/15-gpu-compositor.md). Hang ≠ wipe.

**PRIORITY 0 COMPOSITE GATE** — after REUSE, MUST **scale + gallop-clock + plate-grade + paw contact** ([13](../docs/13-make-bolt-lane.md) / [13b](../docs/13b-anti-sticker-contact.md) / [13c](../docs/13c-green-despill.md) / [13d](../docs/13d-auto-scale.md) / [14c](../docs/14c-gallop-clock.md)) **BEFORE** KEEP / Hang. After key, MUST run `computeScale` / `assertScale` ([`scripts/bolt-scale/`](../scripts/bolt-scale/)) then **`gallop-clock` / `assertGallopClock`** ([`scripts/gallop-clock/`](../scripts/gallop-clock/)) before KEEP. Native 96 fps (no 1-of-N); `assertGallopClock` **FAIL** if dogFps ≪ plateFps. Key-and-hang without proof = **FAIL**.

Hung `biome/master/bolt.mp4` is OUTPUT only — never `@ref` as style teacher.
`lock/bolt-back-prev.jpg` is archive only — never `@ref`.
`lock/bolt-gallop-cycle-0.93s-prev.mp4` is archive only — **FAIL if used as play cycle**.
`lock/bolt-gallop-cycle-24fps-prev.mp4` is archive only — never `@ref` as the cook source.
`lock/RIG-*` / `lock/SEAL-*` / `lock/example-*` / `lock/sill-*` are Citadel hall locks — **NOT** Sprint biome Bolt teacher.
Any local `bolt-rear-*.jpg` invented in a Build sandbox is banned.
